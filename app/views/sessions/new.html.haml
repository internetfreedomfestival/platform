%section
  .page-header
    %h1 Welcome to the IFF Community Platform
    %h5 We’re happy to see you here! You must sign up to the platform to before you are able to take advantage of the following features:
    %ul
      %li Register to attend the IFF
      %li Submit session proposals to the IFF
      %li Apply to the Diversity and Inclusion Fund to receive travel support for the IFF
      %li See the full program and schedule
      %li Leave your session comments and feedback
    %p Please contact <a href="mailto:team@internetfreedomfestival.org">team@internetfreedomfestival.org</a> if you have any issues signing up!
  .row
    .span8
      %h1 Sign in
      = render "shared/flash", flash: flash
      = simple_form_for @user, url: session_path do |f|
        %fieldset.inputs
          = f.input :email
          = f.input :password
          = f.input :remember_me, as: :inline_boolean
          - if params.key?(:return_to)
            = f.hidden_field 'return_to', value: params[:return_to], name: "return_to"
        .actions
          = f.button :button, "Sign in", class: 'primary'
    - if @conference
      .span8
      .span8
        %h2 Don't have an account yet?
        /%h4 Sign up here
        = simple_form_for(@user, url: cfp_user_path) do |f|
          %fieldset.inputs
            = f.input :email
            = f.input :password
            = f.input :password_confirmation
            %p{style: "font-size: 15px; font-weight: bold;"} Please contact us if you don’t receive a confirmation email within 10 minutes after signing up! And don’t forget to check your spam folder ;)
          .actions
            = f.button :submit, t("cfp.sign_up"), class: 'primary'
            = link_to t("cfp.back"), new_cfp_session_path, class: "btn"
  .row
    .span8
      %h2=t("cfp.forgot_password_headline")
      %p=t("cfp.forgot_password_description")
      = action_button "", t("cfp.forgot_password_button"), new_cfp_user_password_path
    .span8
      %h2=t("cfp.confirmation_headline")
      %p=t("cfp.confirmation_description")
      = action_button "", t("cfp.confirmation_button"), new_cfp_user_confirmation_path
